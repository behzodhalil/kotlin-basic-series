package section_02

fun main() {
    // Pair(number, name) -> val (number,name) -> number = 1; name = one
    val (number,name) = 1 to "one"
    // This feature is called a destructuring declaration
}